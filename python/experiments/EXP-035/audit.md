# Audit Report: Experiment EXP-035

## Summary

- **Verdict**: PASS after rerun (original pre-rerun verdict: FAIL — see "Re-Audit (2026-05-29)" at the end)
- **Critical Issues**: 1 (found and resolved)
- **Warnings**: 0
- **Info Notes**: 1

> The body below records the original pre-rerun audit that found the Critical no-collapse inversion. The bug was patched, the experiment was rerun, and the **Re-Audit (2026-05-29)** section at the end verifies the corrected outputs and carries the current PASS verdict.

## Code Review

| File | Check | Verdict | Notes |
|------|-------|---------|-------|
| `code/run_experiment.py` | Scope compliance | PASS | Implements the readiness-only Market Bias port and episode-count survey; no returns or P&L are computed. |
| `code/run_experiment.py` | Holdout exclusion | PASS | Loads data through `load_analysis_timebars`, excluding the final 30 percent before aggregation. |
| `code/run_experiment.py` | Look-ahead prevention | PASS | Aggregation is completed-window based; Market Bias EMAs are causal; chart-timeframe mode avoids MTF lookahead semantics. |
| `code/run_experiment.py` | Result validity | FAIL | The generated result files were produced with an incorrect no-collapse predicate, causing every finite `DominantShare <= 0.95` row to fail `Check5NoCollapse`. |
| `python/src/market_bias.py` | Port determinism | PASS | The port is deterministic and implements the scoped local formula, including the `xhaopen[1]` recursion and two-seeding warmup rule. |
| `python/src/market_bias.py` | Reference fidelity | PASS WITH CAVEAT | No exported TradingView reference series is present, so the experiment may claim deterministic re-implementation only, not Pine equivalence. |

## Numerical Validation

### Spot Checks

- The generated `results/readiness_table.csv` contradicts the no-collapse rule in every row. Example: line 18 (`BTCUSD 1h strict Train`) has `DominantShare=0.5498807104`, which is below the `0.95` collapse threshold, but `Check5NoCollapse=False`.
- Line 19 (`BTCUSD 1h strict Test`) also has `DominantShare=0.5698795181 <= 0.95`, but `Check5NoCollapse=False`.
- A diagnostic recomputation from the existing CSV counts shows that, if only the no-collapse predicate is corrected, `BTCUSD 1h strict` would pass both train and test, and `BTCUSD` plus `USTEC` would pass `1h tolerant`. This is diagnostic only; the official result files must be regenerated by running the experiment code after the fix.

### Range Checks

| Metric | Expected Range | Actual Range | Pass? |
|--------|----------------|--------------|-------|
| Determinism checks | all true for trusted output | all 32 rows true | YES |
| Warmup convergence | true for ready cells | all 32 rows true | YES |
| Row floors | true for ready cells | all 32 rows true | YES |
| Episode floors | may fail for persistent states | 25 of 32 rows fail | YES |
| No-collapse check | true when `DominantShare <= 0.95` | 32 of 32 rows incorrectly false | NO |

## Assumption Validation

| Method | Assumption | Holds? | Evidence |
|--------|------------|--------|----------|
| Chronological split | Higher-timeframe rows remain time ordered | YES | Code sorts via aggregator and splits by row/time order. |
| Episode counts | Long-memory states are serially dependent | YES | Episode counts are reported and are the binding denominator. |
| No-collapse gate | Dominant state share above 0.95 means collapse | NOT VALID IN GENERATED OUTPUT | The output evaluates the boolean incorrectly. |

## Scope Compliance

- Analysis plan followed: NO, due to incorrect implementation of readiness check 5 in the generated output.
- Deviations: `Check5NoCollapse` does not implement `DominantShare <= 0.95`.
- Complexity budget: 0 statistical tests / 0 budgeted, 4 plots / 4 budgeted, 1 new module / 1 budgeted
- Holdout exclusion verified: YES

## Issues

### Critical

1. **No-collapse check is inverted by Python singleton comparison**
   - File: `results/readiness_table.csv`, lines 2-33; root cause in the pre-audit version of `code/run_experiment.py`
   - Description: The generated output marks `Check5NoCollapse=False` even when `DominantShare` is finite and below the `0.95` collapse threshold. This came from using `np.isnan(value) is False`; `np.isnan` returns a NumPy boolean, so the identity comparison is false even for finite values.
   - Impact: `PassesAllChecks` and `results/verdict.json` are invalid. The current no-go verdict cannot be interpreted or documented.
   - Fix: Patched `code/run_experiment.py` to use `np.isfinite(dominant_share) and dominant_share <= COLLAPSE_SHARE` in the no-collapse predicate. The experiment must be rerun manually to regenerate `results/readiness_table.csv`, `results/verdict.json`, and plots before Stage 6.

### Warning

None.

### Info

1. **Reference fidelity remains unverified**
   - Description: No exported TradingView reference series is present. This is scoped and acceptable, but any later Market Bias return result must carry the deterministic-only caveat.

## Re-Audit Requirements

Rerun `python/experiments/EXP-035/code/run_experiment.py` after the patched no-collapse predicate, then resume at Stage 5. The re-audit must verify:

- `Check5NoCollapse=True` whenever `DominantShare <= 0.95`;
- `PassesAllChecks` and `results/verdict.json` reflect the regenerated predicate;
- plots are regenerated from the corrected readiness table;
- no new return, P&L, or scope-expanding output appears.

---

## Re-Audit (2026-05-29) — Post-Rerun

**Verdict**: PASS. The Critical issue is resolved; the regenerated outputs are internally consistent and the verdict follows the predeclared rule.

### Freshness

`results/readiness_table.csv`, `results/verdict.json`, and all four `plots/*.png` are timestamped `2026-05-29 09:12:25`, after the patched `code/run_experiment.py` (`09:05:41`). The stale pre-patch outputs were overwritten by a genuine rerun, not edited in place.

### Critical-Issue Resolution

| Check | Result |
|-------|--------|
| `Check5NoCollapse=True` wherever `DominantShare <= 0.95` | PASS. All 32 rows have `DominantShare` in `[0.501, 0.778]` (max `XAUUSD 4h strict Test = 0.7736`), and all 32 rows now read `Check5NoCollapse=True`. The inverted `np.isnan(...) is False` predicate is gone; `np.isfinite(dominant_share) and dominant_share <= COLLAPSE_SHARE` (code lines 271–272) is in force. |
| `PassesAllChecks` consistent with Check1–5 | PASS. `PassesAllChecks=True` only where Check1–5 are all True; the binding failure is now `Check4EpisodeFloor`, never `Check5`. Spot-check: `USTEC 1h strict Train` has `EpBull=30, EpBear=29` → `Check4=False` (floor 30) → `PassesAllChecks=False`, correct; `USTEC 1h tolerant Train` `EpBull=33, EpBear=32` → `Check4=True` → `PassesAllChecks=True`, correct. |
| `verdict.json` reflects the regenerated predicate | PASS. `passes_readiness=true`; `1h/tolerant=[BTCUSD, USTEC]` (>=2 distinct, both segments), `1h/strict=[BTCUSD]` (1), `4h/strict=[]`, `4h/tolerant=[]`. |
| Plots regenerated | PASS. All four PNGs carry the post-rerun timestamp. |
| No scope creep | PASS. Column set unchanged (no return/excursion/P&L/hit-rate column exists); reference block still carries the deterministic-only caveat; 1 module / 0 stat tests / 4 plots unchanged. |

### Numerical Spot Checks (regenerated)

- Determinism (Check1) and warmup convergence (Check2) are `True` on all 32 rows; `W` ranges `300–405`, `WConverge` floored at `300` correctly (e.g. `BTCUSD 1h` `WConverge=100 < 300 → W=300`).
- Episode floor (Check4) is the discriminator: it fails on **25 of 32** rows. Every `4h` cell fails (train episode counts `4–9`, far below the `30` floor); `EURUSD` and `XAUUSD` fail at `1h` under both aggregations (sign episode counts `~24–28` train, below `30`).
- The only cells clearing both segments: `BTCUSD 1h` (strict and tolerant) and `USTEC 1h tolerant`. `USTEC 1h strict` fails because train `EpBear=29 < 30`.

### Verdict-Rule Legitimacy

The aggregate verdict is the predeclared rule (`scope.md` line 68): checks 1–5 on `>= 2` distinct instruments in both segments at `>= 1` timeframe **under an admissible aggregation**, with the binding canonical aggregation explicitly deferred to the mid-phase reflection (`scope.md` lines 23, 49). Reading readiness off `1h/tolerant` is therefore the predeclared mechanical outcome, not a post-hoc cell selection — Gate 6 (no test selection) holds. The aggregation-dependence is a finding for interpretation, not an audit defect.

### Re-Audit Conclusion

PASS. The original Critical no-collapse inversion is fixed and verified on regenerated outputs. No Critical or Warning issues remain. EXP-035 may advance to Stage 6 (interpretation), carrying the deterministic-only reference caveat and the aggregation-dependence finding.
