# Chapter 05–06 Experiment Index

## Infrastructure (ad-hoc)

| ID | Status | Purpose |
|---|---|---|
| INFR-021 | **COMPLETE** 2026-07-25 — EURUSD/XAUUSD/USTEC → `data/catalog_ctrader/`; fence pin independent of Bybit | Ingest chapter-03 cTrader 1m timebars into Nautilus catalog |

## Chapter 06 — active (checkpoint-018)

**Checkpoint-018 OPEN 2026-07-25 — Trade Opportunity Modelling / Capture Geometry.** Family
`CF-VOLDIR-001` `REGISTERED` (unchanged). Checkpoint-017 **CLOSED** 2026-07-25.

Binding premise: *Unconditional direction is dead. Conditional direction is unpowered, not refuted.
Volatility is a multiplier on a direction term, never a substitute for it.*
Identity: `E[net] = p·W − (1−p)·L − cost`, `p_be_net = (L+cost)/(W+L)`,
`edge = p − p_be_net`. The target is **not** `p > 0.5` — an edge exists at `p < 0.5` whenever
`W > L`. `W/L` is the capture branch's natural handle; κ is a non-tradable diagnostic. (KB **L-49**.)

SoT: `.ignore/what-next/alts/opportunity.md`

**Status 2026-07-26: SPDR-018 and SPDR-018B are both COMPLETE AND CLOSED.** The mid-checkpoint
reflection is the next item and is **not** gated by their outcome — SPDR-019/020 are registered and
would run regardless; what 018/018B changed is how they are *parameterised*. Reflection input
package: `docs/experiments-docs/checkpoints/2026-07-25-018-trade-opportunity-capture-geometry/reflection-mid.md`.

**Headline, both universes:** the joint `(p, W, L)` sits **at break-even**, and **nothing clears
`p_be_net`** — 0 of 1,413 powered crypto cells and 0 of 315 powered cTrader cells. `W/L` is **not a
free lever**: it is ~97% the arithmetic mirror of `p` on both universes (R² 0.9667 crypto / **0.9746**
cTrader) and exit geometry moves it 36–67× while `p` moves inversely and the mean does not improve.
The distance to net break-even is **91% (crypto) to 96% (cTrader) cost, not rate**. Two things remain
open, both `UNPOWERED` rather than refuted: **C2 shock-MOMO** (018B neither replicated nor refuted it)
and **C3** (55% of crypto's unresolved population). New lessons **L-50…L-53**, dead ends **P-21…P-25**.

| ID | Family | Status | Purpose |
|---|---|---|---|
| SPDR-018 **(SPDR)** | CF-VOLDIR-001 | **COMPLETE AND CLOSED 2026-07-26 — `HYP-D5` SUPPORTED (powering succeeded; NO gating verdict)**. Code pin `44c720f82af52b8b…`; 37,791 cells / 24,098 signed; **18 HARD checks, 0 failed** (TRIPWIRE-2 + determinism were declared-but-unrun, raised by the analyst, then fixed and re-run — legal 85.34 vs leaky 644.71 bps = 7.55×; sequential vs `--jobs 8` zero columns differ). **1,413 powered signed cells against SPDR-014's 0 of 927**; parent parity 4.5e-13/1.8e-12/9.1e-13/0.0 proves no estimand was re-specified; all 27 residue items carry cells; **3,559 `NOT_RESOLVABLE` delivered as a quantified first-class answer** (median 7.87× short). `(p, W, L)` now measured, identity to 1.46e-11 bps: `p` 0.3887, `W` 128.65, `L` 75.55, `W/L` 1.4844, `p_be_net` 0.4992, **edge −0.0728** (medians; means `p` 0.3781 / `L` 84.69 / `W/L` 1.7548 / edge −0.0860 — `edge` is **not** the difference of the other medians), gross −1.18 bps, **0 of 1,413 clear net BE / 32.5% clear gross**. `W/L` **96.7% mirror-determined**, 67× movable, free residual `log R` negative at the centre (median −0.0301, mean −0.0356) though positive in the same 32.5% of cells that clear gross break-even (identity), 82.8% indistinguishable from the driftless mirror. Counter-outcome: **129 powered negative CI-excl-0 cells, 1 positive** — does **not** route (best flipped +12.93 vs a 13.1–16.0 floor). Live thread: **C2 shock-MOMO M-3 +22.6 bps, pct 0.95, n 505**. Recorded gaps: arm-C parity 72.5% complete, median/trimmed CIs on 1.0% of cells (the three statistics disagree by 13 bps), M-2 span missing on 13.9%, `plots/` empty. `report.md` §3 carries the per-item ledger. **Follow-up threads closed 2026-07-26 (additive addenda; frozen artifacts untouched):** **P4** — C3 is *terminally unpowerable* in its registered form (all 1,946 unresolved cells already fully levered, median 81× short, median cell needs **201 years** of 25-symbol history at its own event rate; 88.3% need >20y) → **unpowerable, NOT refuted**; **P3** — CI fragility swept, seed spans **~4.8% of CI width**, so no read in either run rests on a Monte-Carlo or block artifact (the data was computed on all 37,791 cells and discarded at `cells.py:127`); **P2** — median/trimmed CIs recovered on 451 arm-B cells, and **they reject zero (449/451 and 451/451, all negative) where the mean does not (46/451)**, so the near-break-even framing is the most favourable of the three statistics. **P1 SKIPPED by operator** (no 018C) → **C2 must be booked at the retrospective as unresolved-and-parked, a terminal `NOT_RESOLVABLE`, never a refutation** | Power the complete 017 residue |
| SPDR-018B **(SPDR)** | CF-VOLDIR-001 | **COMPLETE AND CLOSED 2026-07-26 — `HYP-D5` PARTIALLY SUPPORTED** (`HYP-D5`, second universe: cTrader EURUSD/XAUUSD/USTEC, INFR-021 fence, **3 instruments against 25**). 7,578 cells; **11 HARD checks + 1 INFORMATIVE, 0 failed**. **REPLICATION / CREDIBILITY ONLY — never pooled into crypto `n`** (AMENDMENT-C1/S1); cost **DOUBLY SYNTHETIC** (borrowed from Bybit *and* rescaled), gross primary, spread pin still blocking. **Structure replicates, more tightly:** `p` 0.4868 vs `p_be` 0.4855 (gap **+0.0013**), gross mean **−0.080 bps = 0.006σ**, `W/L` mirror **R² 0.9746 / slope 0.9656**, 93% indistinguishable from the mirror, `W/L` 36.4× movable with `p` inverse, cost = **95.8%** of the gap, **0 of 315 clear net BE at ANY charge above 1.39 bps**. **C2 the replication target: NOT REPLICATED AND NOT REFUTED** — the comparator is not a neutral yardstick (its own mean +0.97 EU → +12.05 Asia; Asia null entirely above zero; blind upward), the effect vanishes in EU (+0.62, pct 0.443) and concentrates in Asia (−13.57), and an independent rebuild flips `P-MR` 0.067→0.826. **CORRECTED 2026-07-26:** the fourth original reason — that the like-for-like cell (n 290) could not have detected crypto's +22.6 — is **false**; its plant curve reads 1.000 at +20 and +40 bps, so it WAS powered for an effect that size and measured the opposite sign (−9.383, pct 0.043). That leg now supports *not replicated* and no longer supports *not refuted*. **Three prior headline numbers were wrong and are corrected:** powered 2,401→**315** (absolute 10 bps bar = 0.767σ here vs 0.137σ on crypto — **L-50**), net-clearing 12.9%→**0.0%**, mirror R² 0.311→**0.9746**. Coverage gaps closed natively (C7 627 pairs, C8 339 cells, B3 159 cells — B3 **0 of 159 powered, 16.4× short**). **OPEN / NOT RUN, never nulls: C9, D3, D4.** Recorded: **seven inherited HARD checks do not exist** (determinism among them, on a resumed run; + Bybit-holdout assertion on the §5 guard = the only residual Phase-0 exposure), cost deflator **circular, range 0.185–0.703** (**L-53**), no median/trimmed CIs and no fragility sweeps at all, power flag not regenerable (317 vs 315). Selection artifact verified and now moot (**L-51**). New lead **P6**: Asia magnitude×shock ~+10 bps vs ~0 EU — unregistered | Replicate the 017 residue on a second asset class |
| SPDR-018 — *design record* | CF-VOLDIR-001 | Design as frozen 2026-07-25 (`HYP-D5`). **Powering sweep over the COMPLETE checkpoint-017 residue**, each item in its **original statement**, no omissions: arm A = SPDR-012 residue (V-REGIME-HMM 76/83 unpowered, V-TAIL D1, DESIGN-band date deficit, V-CLOCK D1, §6.4 unsatisfiability); arm B = SPDR-013 residue (stop/trail/time arms, the 125 positive-mean unpowered cells, ZZ structural leg per symbol, M15) — **and where `W`/`L` get measured**; arm C = SPDR-014 residue (0/927 powered residual, shock-MOMO, ordered last-k L→H + `LHL` mirror, E-TOUCH/E-CLOSE, magnitude scaling, z/h dose, DESIGN→CONFIRM flip, rate lean, `DA-STRADDLE` characterisation-only); arm D = SPDR-015 residue (transition counts, run-length, T-GT-MED10/MED5, H4 k=1, R-HMM-RV, D1 stickiness, **the never-scored CONFIRM slice**). Only authorised drop = SPDR-017. Multiplicity disclosed not rationed; reuses parents' `screen_code/`; parent-parity asserted. Uniform layer: `p`/`W`/`L`/`W_L`/`p_be_net`/`edge`, block MDE (not iid). Crypto pooled = powered estimate; cTrader = replication only. **`NOT_RESOLVABLE` is a first-class result** | Power the complete 017 residue |
| SPDR-019 **(SPDR)** | CF-VOLDIR-001 | **DESIGN COMPLETE AND QA-APPROVED 2026-07-29 (QA run 7) — implementation authorised; EXECUTION still unauthorised** (`HYP-D6`). Fixed signed candlestick-breakout entry (SoT §6.1, frozen; stop fills resolved causally on **M1 bars**, unfilled and suppressed signals counted). **Primary read is the exact payoff residual `log R = log(W/L) − log((1−p)/p)`** (slope 1 — the fitted-slope form is refused as a target); the null is the **driftless mirror**, never zero P&L. **Cost excluded from every estimand, threshold and comparison (AMENDMENT-C5)**; `p_be_net` emitted as disclosure only. **Layer protocol (AMENDMENT-C6)**: phase (a) L0 baseline → L1 scale → L2 state (shock, level, joint) → L3 swing gate → L4 devices one at a time, each run **unmodulated and modulated** → L5 evidence-selected combination; phase (b) full cross whose trigger depends on (a) but whose **scope does not** (flat layers retained; estimand = the interaction term). **Resolution is reported as a sensitivity ladder, not a pass/fail bar (AMENDMENT-C7):** every cell emits its block MDE, CI width and detection rate at `{0.02, 0.03, 0.05, 0.075, 0.10, 0.15}` log units, and **no `powered`/`unpowered`/`NOT_RESOLVABLE` flag is emitted anywhere** — adequacy is the reader's judgement. Powered by full TRAIN + M15 (H1 co-report); **expected episode counts are explicitly unknown until measured** — no parent ever ran this entry, so every one of the 5,148 predeclared strata carries a null expected `n` rather than a date-range guess; pooled by construction, per-symbol as disclosure | Opportunity-modulated capture geometry on a fixed entry |
| SPDR-020 **(SPDR)** | CF-VOLDIR-001 | **DESIGN COMPLETE AND QA-APPROVED 2026-07-29 (QA run 7) — implementation authorised; EXECUTION still unauthorised** (`HYP-D7`). SPDR-014 E-TOUCH/E-CLOSE/E-HORIZON event grammar inherited, with parent parity asserted at `z=1.5`. No selectivity gate: every declared signal is reported and `p_event` is a covariate, never a filter. Expected resolution is pre-generated at the exact signed-arm grain: only the parent parity anchor has known counts (P-MOMO 15,041; P-MR 15,331); every other declared base stratum is explicitly unknown until measured. Full TRAIN is primary; DESIGN/CONFIRM are verification reads | Direction-aware capture on the event object |

## Chapter 06 — checkpoint-017 (CLOSED 2026-07-25)

Closed as `STRUCTURAL PACKAGE DELIVERED / EXTRACTION UNRESOLVED-AT-POWER`. 0 counted TEST reads;
0 multiplicity slots; family status unchanged; XENA never opened.
Retrospective: `docs/experiments-docs/checkpoints/2026-07-23-017-structural-vol-direction-programme/retrospective.md`

Sequence brief: `.ignore/what-next/alts/cf-voldir-o3-zone-event-sequence.md`

| ID | Family | Status | Purpose |
|---|---|---|---|
| SPDR-012 **(SPDR)** | CF-VOLDIR-001 | **COMPLETE** — vol level H1/H4 reliable; no within-day skill | Volatility characterisation |
| SPDR-013 **(SPDR)** | CF-VOLDIR-001 | **COMPLETE** — signed direction not adequate; ZZ mag IC 0.34–0.46 | Direction expectancy bps |
| SPDR-014 **(SPDR)** | CF-VOLDIR-001 | **SCREEN COMPLETE** (operator disposition 2026-07-24) — analyst INCONCLUSIVE / UNPOWERED_NOT_NULL (B-5); `residual_status=NONE`, 0 powered cells; integrity PASS; band non-selective (p_event≈1), continuation ≈ coin-flip; coherent SUGGESTIVE leads (shock-MOMO CI excl 0, E-TOUCH/E-CLOSE asymmetry, L→H vol-flip MOMO); **SPDR-016 OPENED by operator override** (`016_start_basis=OPERATOR_OVERRIDE`); 0 TEST reads | O3 primary product science |
| SPDR-015 **(SPDR)** | CF-VOLDIR-001 | **SCREEN COMPLETE — WORTH_EXPLORING (per-arm; operator-signed 2026-07-24)** — Group 2 conditioners. **2b ordinal swing-size gate** T-GT-CUR **WORTH_EXPLORING** (21/21 coins × 3 models; hit ~+20pt over base, IC≈0.37, CI-backed) — MED5 weaker WORTH_EXPLORING, MED10 INCONCLUSIVE. **2a R-MARKOV multi-bar level gate k=4/12 WORTH_EXPLORING** (16/16 coins H1; ΔBrier −0.025 k4 / −0.114 k12 = ~15%/33% less error than persistence, CI excl 0) + **HIGH/LOW vol-state labels WORTH_EXPLORING** (next-\|oo\| gap +35 bps HMM / +16 bps R-MARKOV). **2a k=1 next-bar NOT_WORTH** (thin; H4 k1 + R-HMM-RV forecast); R-SHOCK comparator only. Integrity PASS (hard_pass; golden G1–G4); control = true L-28 derangement both arms (collapse≈0; bite 98%/73%); QA run1 REVISE→run2 APPROVE. Fold into 014/016 by amendment only; no family status change; no XENA; 0 TEST reads | Conditioner science |
| SPDR-016 **(SPDR)** | CF-VOLDIR-001 | **CLOSED — SUPERSEDED, NEVER RUN** (2026-07-25 retrospective §4). Was DESIGN COMPLETE / opened by operator override 2026-07-24. Superseded on two recorded grounds: (i) its DERIVED error-dynamics feature layer was independently measured **inert** by SPDR-017 (A1−A0 median −5.8 bps; 5/16 improve); (ii) its actual target — powering the 014 leads — is carried forward inside **SPDR-018** arm C, in the original event-nested form. Supersede-and-retain: the design stays on disk. **0 reads consumed** | Refine named 014 residual (superseded) |
| SPDR-017 **(SPDR)** | CF-VOLDIR-001 | **CLOSED — NOT_WORTH** (operator-signed 2026-07-24) — Group 3b independent mispricing; residual_status=NONE; integrity PASS; no model skill (IC≈0), DERIVED layer inert, M-ZONE ≤ Z-VOL; per-stratum UNPOWERED (B-5) | Predicted-price mispricing + 014 method |
| XENA-VOLDIR-001 | CF-VOLDIR-001 | **RESERVED** | Portfolio/search on graduated bases |

See checkpoint-017, `reflection-mid.md`, and `docs/references/chapter-06-governance.md`.

## Chapter 05 — closed path (no further authorised work)

| ID | Family | Status | Purpose |
|---|---|---|---|
| SPDR-011 | CF-VOLCONV-001 | **CLOSED** — L1 **NOT SUPPORTED**; see `report.md` | Partial-cost characterisation only |
| EXP-099 | CF-VOLCONV-001 | **RESERVED / NOT AUTHORISED** | Frozen-rule Nautilus reproduction |

Checkpoint-016 retrospective (CF-VOLCONV-001) still pending and independent of Chapter 06.
