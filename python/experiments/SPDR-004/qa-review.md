# SPDR-004 — QA review log (append-only)

## QA run 1 — 2026-07-16T20:37Z — mode: subagent — HEAD f41ba32 — POST-EXECUTION (SPDR lane)
Verdict: **REVISE** (disclosure/method items; no integrity breach — fence, causality, golden traces all independently verified)

Scope note: SPDR lane replaces pre-exec QA with the design §10 code-asserted checklist; this review verifies post-hoc that the substitution held. Reviewed state: working tree at HEAD f41ba32 with SPDR-004 untracked.

### 1. Design-fidelity trace

| Design clause (§ref) | Code (file:line) | Verdict | Notes |
|---|---|---|---|
| §4 estimand `r_bps = s·(O[t+H]−O[t])/O[t]·1e4`, open-to-open, non-overlapping | `screen_code/spdr004_screen.py:582-601` (trade_returns), `:498-509` (greedy active-hold) | MATCHES | Spot-regenerated BONK 4h UNF DI_ADX h1: mean 57.308662 bps, n=621 — byte-identical to cells.parquet |
| §4 RAND ≥25-seed battery, rank read | `:58` seeds 1000–1024 (25), `:643-687`, rank `:1094-1114` | MATCHES | Battery baseline regenerated byte-identical (−4.604603 bps, 25 seeds) |
| §4 lift = treatment − matched baseline, **paired lift CI** (§7 Control A "paired lift CI") | `:1117-1123` | **DEVIATES** | Lift CI = treatment CI − fixed baseline mean; baseline uncertainty ignored → anti-conservative on the 29 lift_ci_low>0 cells that feed K=3 clause 1. Disclosed in analysis.md §1/§3.12 but is a design deviation, not an approved amendment |
| §4 L-20: `block_sensitivity` ½×/1×/2× + `ci_low_seed_range` **disclosed** | imported `:26-31`, computed `:624,636-638` | **MISSING in results** | `block_sensitivity` never called; `block_h_ci_low/high` and `ci_low_seed_range` computed in `summarize_returns` but **dropped** from the emitted row (`:1150-1184`) — cells.parquet has neither column. Integrity item 9's printed detail "block≥H disclosed" is factually wrong w.r.t. results/ |
| §5 unit pin from TRAIN at screen start | `:693-719`, `:989-1018` | MATCHES | unit_pin.json measured_at 20:13:27 (run start 20:12:05); values recomputed-plausible (SHIB 1h/5m median ATR 28.9 bps over 122,656 bars; BONK 93.2/172.8/362.8 bps) |
| §5 money floor via `bybit_round_trip_cost_bps` | `:997-1005` | MATCHES (disclosed placeholder) | spread_bps=2.0 GAP placeholder, labeled as such |
| §6.1 selection: daily 00:00 UTC, trailing 24h volume ≤ t−1, tie-break lex, top-10 | `:279-357` | MATCHES | Window `[D−24h, D)` on CloseTime (strict `side="left"` at D → ≤ t−1); rank ordinal over pre-sorted (vol desc, symbol asc) = lex tie-break; **G3 recomputed independently** for 2023-04-02: exact 10/10 rank match |
| §6.3 grid: 3 domains, holds {0.5,1,2,4}×, bases UNF/MOM/RAND, filters DI/DI_ADX, 720 cells | `:46-58`, run log | MATCHES | 720 treatment + 240 baseline cells emitted; H tables match ratio×mult |
| §6.3 "Symbols: online top-10 — 10 (time-varying)" | `:63-64,943-952` | **DEVIATES (interpretive)** | Cell grid restricted to fixed top-10 by **membership-days** out of 77 ever-members; per-bar member[t] gating retained. Reconciles the contradictory "720 fixed cells × time-varying 10" design text, but is an undeclared interpretive choice — no AMENDMENT ledger entry, disclosed only post-hoc in screen.md §1. 67 ever-member symbols never measured |
| AMENDMENT-1: UNF baseline = RAND battery @ UNF cadence; no UNF×NONE anywhere | `:536-537` (raises on UNF×NONE), `:1075-1076,1089-1099`, baseline rows `:1187` only MOM/RAND | MATCHES | Confirmed no UNF×NONE row in cells.parquet (240 baselines = 2 bases × 120) |
| §6.3 MOM lookback N = 1 HTF span, sign(C[t−1]−C[t−1−N]) | `:430-445` | MATCHES | N=ratio; indices causal (t−1) |
| §6.3 ADX≥25, period 14, seeds {1000..1024} | `:55-58,541` | MATCHES | |
| §7 Control C: phase-shift K=50 HTF bars | `:59,397-401,976-978,1125-1139` | MATCHES | `np.roll(+50)` with wrap head invalidated (dir=0, adx=NaN) — labels lagged 50 HTF bars, no leak. Shift, not permutation, so L-28 N/A on the emitted path; derangement helper (`:258-267`) exists, is asserted 0-fixed-points (`:1021-1041`), and a cyclic shift is itself fixed-point-free |
| §3 HTF CloseTime < Open(t), no forming bar | `:253-255` (`side="left"` −1), assert `:971-974` | MATCHES | **G1 recomputed independently from catalog 1m**: htf_close 1657947600e9 < ltf_open 1657947900e9, next HTF close ≥ open, DI sign +1, r_bps(H=12) = −121.04283054003717 — exact match to integrity.json |
| §10 checklist "code-asserted" | `:811-903,1273-1312` | **PARTIAL** | Items 4, 9, 11 are hardcoded `True` (`:1049,1265,1266`) — printed PASS without an assertion. Item 4 is causal-by-construction (verified via G3); item 11 verified here by `check_no_local_accounting` (ok=True both dirs); item 9's claim is partially false (see L-20 row above). Gate does hard-exit before promote-facing writes on any FAIL (`:1310-1312`) and cells.parquet written only after 11/11 (`:1314-1316`) |
| §11 golden G1 "mid-TRAIN day" | `:722-747` | MINOR DEVIATES | Code takes first valid bar = 2022-07-16, which is day 2 of available data (catalog 1m effectively starts 2022-07-14/15; membership spans 2022-07-15 → 2023-12-18, 522 rebalances — not the full 2021-06-29 TRAIN band). Data-availability fact, not a fence issue |
| §13 artifacts incl. plots/ | — | MISSING (minor) | No plots/ directory produced |

### 2. Golden-trace diff (independent recompute)

- **G1** (SHIB1000USDT 1h/5m): rebuilt from fenced catalog 1m via aggregate_ohlc; ltf idx 318, HTF boundary strict, forming bar unused, DI sign +1, r_bps H=12 = **−121.04283054003717** — bit-identical to screen emission. PASS.
- **G3** (2023-04-02 00:00 UTC): recomputed trailing-24h volume over the full 612-symbol admitted pool directly from 1m bars with ts_event ∈ [reb−24h, reb): top-10 order **exactly matches** membership.parquet (1000BONK, SHIB1000, RSR, DOGE, ANKR, SLP, XRP, GALA, ZIL, JASMY). PASS.
- **G2**: screen-internal check (ADX 34.32 ≥ 25, boundary ok); logic verified by code read; not independently recomputed (G1 covers the boundary mechanism on the denser domain).

### 3. Governance & boundary (evidence)

- **TRAIN fence**: the only catalog read is `fenced_bar_query(..., band="TRAIN")` at `:158-165`; no other `query` call in screen_code or analysis_code; analysis_code reads only results/*.parquet. `max_exit_ns=1702857000e9 < train_end 1702857600e9` (2023-12-17 23:50 < 12-18 00:00). Holdout_start 2025-01-08 never referenced. **No breach.**
- **check_no_local_accounting**: `{'ok': True}` on both screen_code/ and analysis_code/ (rerun by this reviewer).
- **Seeds**: `draw_rand_sign` regenerable (verified identical draws); 25 seeds 1000–1024.
- **Headline number recompute from cells.parquet** (all match run log/screen.md/analysis.md exactly): 960/720/240 rows; med mean 1.3814, med lift 1.4677; 29 lift_ci_low>0; 119 unpowered; per-domain medians (−0.12/+0.02, 2.60/2.15, 10.25/9.99) and BONK 4h UNF DI_ADX ladder (27.30/57.31/129.16/306.74 bps) all reproduce.
- **No disposition written by screen or analyst**: screen.md and analysis.md both defer to operator; analysis.md presents evidence for AND against, flags Control C non-collapse and BONK concentration. Compliant.
- **L-23 amendment ledger**: AMENDMENT-1 recorded NEUTRAL (1/0/0 directional count OK); the PRIMARY_N interpretive choice and the unpaired lift CI are **not** in the ledger (see Issues).

### 4. Issues

1. **[REVISE — L-20 disclosure missing from results]** design §4 / §10 item 9; `spdr004_screen.py:636-638` vs `:1150-1184`. `block_h_ci_low/high` and `ci_low_seed_range` are computed then dropped; `block_sensitivity` (imported `:26`) never called. Integrity item 9's PASS detail ("block≥H disclosed") is not true of the emitted artifacts. Required change: re-emit cells (or a promote-cell supplement) with block≥H CI, ci_low_seed_range, and block_sensitivity on the 29 CI-positive cells — or operator explicitly waives on the non-overlapping-trades argument before disposition.
2. **[REVISE — unpaired lift CI]** design §7 Control A "paired lift CI"; `:1117-1123`. Treatment-CI-minus-fixed-baseline understates lift uncertainty; the 29-cell CI-clear count feeding promote clause 1 is anti-conservative. Required change: paired/bootstrap lift CI on promote-candidate cells, or disposition must treat lift_ci_low>0 as approximate (analysis.md already flags this — bind it at the operator gate).
3. **[Advisory — checklist items asserted by fiat]** `:1049,1265,1266`: items 4/9/11 hardcoded True. Items 4 and 11 independently verified true by this review; the pattern defeats the SPDR "code-asserted" substitution and should be fixed in any re-run.
4. **[Advisory — undeclared interpretive deviation]** PRIMARY_N fixed top-10-by-membership-days (`:63,943-952`) vs §6.1 time-varying strata; add a dated amendment entry (direction NEUTRAL/TIGHTER) if the screen is cited.
5. **[Minor]** G1 sits at data start, not "mid-TRAIN" (§11); no plots/ (§13); effective data window 2022-07-15→2023-12-18 should be stated wherever "TRAIN 2021-06-29→2023-12-18" is quoted.

No fence, causality, leak, or holdout finding. REVISE items are method/disclosure, already surfaced (items 1–2) in analysis.md §1/§6; they gate the *promote read*, not the artifact's integrity.

---

## QA run 2 — 2026-07-16T22:09Z — mode: subagent — HEAD f41ba32 — POST-EXECUTION rerun (AMENDMENTS 2–4)
Verdict: **REVISE** (one run-1 finding replaced by a new, larger CI-calibration defect on the UNF arm; no integrity breach — fence/causality/golden traces re-verified on the rerun)

### Per-finding resolution (vs run 1)

| Run-1 finding | Status | Evidence |
|---|---|---|
| 1. L-20 emission gap | **RESOLVED** | `block_h`, `block_h_ci_low/high`, `ci_low/high_seed_range_lo/hi`, `block_sens_{half,1x,2x}_ci_low`, `lift_block`, `lift_ci_method`, `lift_ci_low_seed_range_lo/hi` all present in cells.parquet; `block_sensitivity` genuinely called (`spdr004_screen.py:650-653`) at ½×/1×/2× of block_h; 624/624 powered treatment cells have finite `block_h_ci_low` and `block_sens_1x_ci_low` (recomputed); 0 powered CI+ cells show block_h→2× sign flip on the mean CI (analysis.md's "7 fragile" uses its own subset definition — consistent order) |
| 2. Unpaired lift CI | **PARTIALLY RESOLVED — NEW DEFECT** | MOM: proper two-sample block bootstrap (`two_sample_lift_ci`, :678-732) — regenerated SOL 4h h1 MOM DI bit-identical (5.09998 / [−6.078, +15.763]); MOM CI+ count dropped to 9/240, confirming the old construction's anti-conservatism. **UNF: `battery_minus_seeds` (:735-762) bootstraps only the 25 seed means and treats the treatment mean as fixed — treatment sampling error omitted.** See Issue 1 below |
| 3. Hardcoded checklist items | **MOSTLY RESOLVED** | Item 9 now a real assertion (column presence + finite-coverage + no-unpaired-method residual, :1455-1486); item 11 now an AST self-scan for adjudication imports (:1488-1500), independently confirmed by `check_no_local_accounting` (ok=True both dirs). Item 4 remains soft (:1190-1197 checks column presence + min≥0, not a causality property) — advisory only: causality holds by construction (`[D−24h, D)` strict right bound, :313-314) and was re-verified by independent G3 recompute |
| 4. Undeclared fixed top-10 strata | **RESOLVED** | AMENDMENT-3 in design §12b declares it, DIRECTION: NEUTRAL; §6.1 carries the cell-strata clause; running count updated (0 looser / 1 tighter / 3 neutral); summary.json discloses `primary_stratum_rule` |
| 5 (minor). Effective window / plots | **PARTIAL** | design §6.1 + summary.json now disclose the ~2022-07-15 catalog start; plots/ still absent (minor) |

### AMENDMENT-2 (notional rank key) verification

- Code matches amendment text: notional = 1m volume×close over `[reb−24h, reb)` CloseTime-strict (`:305-316`), rank on `trailing_24h_notional_usdt`, lex tie-break (`:338-348`); membership.parquet carries both notional and base-volume columns (5217 rows, 522 rebalances, 2022-07-15→2023-12-18).
- **G3 independently recomputed** for 2023-04-02 over the full 612-symbol pool from raw 1m bars (vol×close): top-10 exactly matches membership.parquet (BTC, ETH, XRP, DOGE, ARB, SOL, ZRX, ICX, ANKR, LTC).
- Primary strata now liquid majors (BTC/ETH/SOL/XRP/OP/DOGE/PEPE/APT/LTC/LINK) — corrects the run-1 meme-name confound.

### Silent-change scan (run-1 code vs rerun code)

Diff confined to: A2 (daily_volume_series, build_membership, golden_trace_g3), A4 (summarize_returns L-20 fields, two_sample_lift_ci, battery_lift_ci, eval_rand_battery, cell-row emission), item 4/9/11 assertion rewrites, summary disclosure keys, removal of the unused `collapse_fraction` import. Entry logic, estimand, greedy hold, Control C (K=50 shift), seeds, fence read path, AMENDMENT-1 guard (UNF×NONE raises; 0 rows in cells.parquet), G1/G2 logic — all unchanged. **No undeclared behavior change found.**

### Recomputations (rerun artifacts)

- Headline: 960/720/240 rows; med mean +0.2022, med lift +0.7904, **219** lift_ci_low>0 (202 powered), 96 unpowered — all match summary.json/screen.md/analysis.md.
- CI+ by method (recomputed): `two_sample_block` (MOM) 9/240; `battery_minus_seeds` (UNF) **108/240**; `two_sample_seed_means` (RAND) 102/240.
- Spot regens bit-identical: MOM SOL 4h h1 DI lift CI; UNF SOL 4h h1 DI_ADX lift 12.0175 / [10.3508, 13.7236].
- unit_pin.json re-measured at run time (20:52:44 vs start 20:51:24); BTC medians 13.5/26.6/58.0 bps plausible.
- G1 (rerun, ltf_bar 349 under new membership): boundary and forming-bar checks consistent; r_bps −64.138 emitted (same HTF bar as run 1's independently-verified boundary).
- No disposition stamped by screen.md or analysis.md; analyst presents FOR and AGAINST; no registry/TEST/holdout touch.

### Issues

1. **[REVISE — UNF lift CI omits treatment sampling error; contradicts AMENDMENT-4 text]** design §12b A4 ("two-sample block-bootstrap CI of mean(treatment)−mean(baseline)") vs `battery_lift_ci` (`spdr004_screen.py:735-762`): only the 25 seed-mean diffs are bootstrapped (block=1, n=25), so the CI half-width ≈ battery-mean SE alone. Recomputed on SOL 4h h1 UNF DI_ADX: treatment SE ≈ 6.2 bps vs battery-mean SE ≈ 0.87; emitted lift CI half-width 1.69 bps vs proper two-sample ≈ 12.3 bps; proper ci_low ≈ **−0.27** vs emitted **+10.35** — the cell flips. Approximate proper two-sample re-screen over all 108 UNF CI+ cells: only **≈7** survive. The A4 ledger tags the amendment TIGHTER, but this arm became dramatically **looser** than even the run-1 construction (which at least carried treatment CI width) — an L-23 direction-tag violation in effect. analysis.md §3.12 asserts "A4 unpaired CI removed? Yes" and §4.1 leans on "219 (202 powered)" as top evidence FOR without this caveat. Required change: compute UNF lift CI as a genuine two-sample bootstrap (treatment trade series, block≥H, vs battery), re-emit, and re-derive the CI+ count; or the operator must read UNF/RAND "CI+" as rank-style evidence only (L-19 battery percentile), never as a bootstrap-CI claim. Note the practical read direction is unchanged: Control C already destroys most CI+ mass and analysis.md's framing leans against tradability promotion — but the promote-facing number itself is miscalibrated.
2. **[Advisory]** `two_sample_seed_means` (RAND, :1253-1262) is battery-vs-battery on seed means — internally consistent for "does the HTF-filtered battery beat the NONE battery on this sample", but both arms share the same price path, so the CI does not cover common-sample variation; should be labeled a conditional/sample-fixed CI in any promote language (102 of 219 CI+ ride on it).
3. **[Advisory — carried]** integrity item 4 still not a genuine causality assertion (:1190-1197); construction is causal and externally verified, but the "code-asserted" claim remains partially fiat.
4. **[Minor — carried]** plots/ absent (design §13).

Integrity of the rerun (fence, causality, membership, seeds, no-local-accounting, no-disposition) is clean; REVISE is scoped to Issue 1 before any promote-facing use of the lift-CI counts.
