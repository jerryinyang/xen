# EXP-018 — CF-MR-005 HYP-003 disposition probe: deliberate ladder harvest (PRICE-PRIMARY, 4h).
# LIVE both-leg market cell, arm A semantics = joint form-1 (spread reverts through the moving mean) + group 3xHL cap-48 time-stop (operator-resolved 2026-07-04); reentry forced none (residue object).
# Series S8_RVINDEX; ladder z*={1.5,2.0,2.5} (CisZStar=1.5); native cTrader orders (Mode=3), m1 fills.
# Fence = per-instrument 49% TRAIN cutoff (EXP-013/EXP-016 TRAIN_FENCE). TRAIN band only; TEST/holdout
# never emitted. 0 counted reads. Analysis-only Python on this emission (L-01).

MODE=3
BALANCE=100000000
STRATEGY="cross_instrument_spread_mr"
STRATEGY_VALUE="5"

SYMBOLS=(US500)
DOMAINS=(4h)

ANALYSIS_END=(
  [US500]="2024-09-17T17:26:00Z"
)
BACKTEST_END=(
  [US500]="17/09/2024 17:26"
)

MODEL_ARGS_BY_SYMBOL=(
  [US500]="--CisSeries=S8_RVINDEX --BasketMates=USTEC;US2000;JP225 --CisZStar=1.5 --CisExitSet=harvest --CisBothLeg=true --CisBothLegEntry=market --CisReentry=none"
)

MODEL_ARGS=()  # constants are frozen model constants — no global params.
