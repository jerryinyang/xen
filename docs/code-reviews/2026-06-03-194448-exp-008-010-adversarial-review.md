Used `research-pipeline` references: shared pipeline config, dataset reference,
architecture reference, `python/experiments/INDEX.md`,
`docs/experiments-docs/INDEX.md`, and the active checkpoint design
`docs/experiments-docs/checkpoints/2026-06-03-002-referee-refinement-and-stringency/design.md`.

Content type: empirical experiment design plus pre-execution analysis code.
Active lenses: statistical validity, implementation faithfulness, predeclaration
and governance integrity, reproducibility, holdout discipline, and scope control.

Review scope note: EXP-008, EXP-009, and EXP-010 currently contain `scope.md`,
`analysis-plan.md`, code, and pre-execution governance only. None has `results/`,
`audit.md`, `results.md`, `report.md`, or post-experiment governance in this
workspace. This is therefore a pre-execution adversarial review. I did not run
any experiment orchestration or generate results.

Verification performed:

- Read the required pipeline references and the active Phase 002 `design.md`.
- Reviewed all EXP-008 through EXP-010 scope, plan, code, and pre-execution
  governance files.
- Checked the package state with `find`; no post-execution artifacts exist.
- Ran `PYTHONPATH=python/src python3 -m py_compile` on the EXP-008/009/010 code
  files. It passed.
- Ran `cd python && uv run ruff check ...` on the EXP-008/009/010 code files. It
  passed.
- Ran small pure-function checks under `uv` for EXP-009 classification and
  EXP-010 fold/comparison helpers only. No experiment code path was executed.

```json
[
  {
    "id": "F01",
    "severity": "Major",
    "title": "EXP-010 suppresses FPR-only material failures when MDE is not reportable",
    "evidence": "EXP-010 scope defines a material protocol change if either the MDE margin is breached, the FPR Wilson interval is disjoint, or the protocol FPR Wilson lower bound exceeds alpha0 (`python/experiments/EXP-010/scope.md:15-23`; `python/experiments/EXP-010/analysis-plan.md:142-145`). The implementation computes `fpr_disjoint` and `fpr_uncontrolled`, but then gates the final `material` flag on `reportable`, which requires the protocol MDE row to have `status == \"PASS\"` (`python/experiments/EXP-010/code/run_experiment.py:460-480`). A synthetic call to `compare_protocols()` with `fpr_disjoint=true`, `fpr_uncontrolled=true`, and protocol MDE status `FPR_UNCONTROLLED` returned `material=false` and domain verdict `SUPPORTED`.",
    "impact": "An alternative split protocol can exceed the false-positive criterion and still be reported as non-material if its MDE is not estimable. That directly inverts H-split for the most important failure mode: uncontrolled FPR.",
    "fix": "Compute FPR materiality independently from MDE reportability. Mark a protocol material whenever `fpr_disjoint` or `fpr_uncontrolled` is true with usable FPR precision, even if the MDE row is `FPR_UNCONTROLLED`, `INCONCLUSIVE_NO_CROSSING`, or otherwise non-PASS. Keep a separate MDE-reportability flag for MDE deltas."
  },
  {
    "id": "F02",
    "severity": "Major",
    "title": "EXP-010 pooled multi-fold train and OOS sets overlap",
    "evidence": "The analysis plan requires train/test index sets to be disjoint and the purge/embargo gap to be present (`python/experiments/EXP-010/analysis-plan.md:180-185`). `split_protocols._pooled_indices()` deduplicates the union of every fold's train indices and every fold's test indices (`python/experiments/EXP-010/code/split_protocols.py:112-119`), then `evaluate_partition_referees()` uses those pooled indices for block length, L1/L4, and OOS bootstrap inputs (`python/experiments/EXP-010/code/split_protocols.py:237-257`). A toy 100-row check produced overlap of 40 rows for walk-forward and 100 rows for purged CV.",
    "impact": "Rows that are OOS for one fold enter the pooled in-sample set through another fold. That lets L4 train stability and block-length estimation see observations also scored as pooled OOS, so the wrapper is no longer a clean train/test partition of the frozen referee. Protocol FPR/MDE shifts may reflect this aggregation artifact rather than true split robustness.",
    "fix": "Do not collapse multi-fold protocols into overlapping pooled train/test unions. Either evaluate the frozen referee per fold and combine fold-level outputs under a predeclared aggregation rule, or define a single disjoint pooled partition for each protocol and enforce `train_idx intersection test_idx = empty` before emitting verdicts. Re-run the reference-reproduction check after the change."
  },
  {
    "id": "F03",
    "severity": "Major",
    "title": "EXP-009 MDE-location labels ignore the CI rule in the approved plan",
    "evidence": "The analysis plan defines `below_MDE` as `effect + its CI upper bound < domain MDE`, `near_MDE` as within grid uncertainty, and `at_or_above_MDE` as `effect >= MDE` (`python/experiments/EXP-009/analysis-plan.md:91-94`). The implemented `classify_location()` accepts only `effect`, `mde`, and `grid_unc`; it does not receive or use `ci_upper_bps` (`python/experiments/EXP-009/code/run_experiment.py:185-202`). The distribution summary counts `location_vs_mde` labels directly (`python/experiments/EXP-009/code/run_experiment.py:303-305`), while `ci_upper_below_mde` is only a side field in `location_rows` (`python/experiments/EXP-009/code/run_experiment.py:263-275`).",
    "impact": "A strategy whose point estimate is below the MDE but whose uncertainty interval crosses the MDE will be counted as `below_MDE`. This can overstate the conclusion that the broadened untuned strategy set sits safely below the MDE map.",
    "fix": "Make the classifier CI-aware. At minimum, label `below_MDE` only when `ci_upper_bps < mde_bps`; otherwise classify the cell as `near_MDE` or an explicit uncertainty category unless the point estimate meets `at_or_above_MDE`. Use that CI-aware label in `effect_distribution_summary.csv` and plots."
  },
  {
    "id": "F04",
    "severity": "Major",
    "title": "EXP-009 dependency gate does not enforce required upstream validity",
    "evidence": "EXP-009 scope requires EXP-003 `overall_status == \"COMPLETE\"` with finite gate-stack MDE rows and EXP-004 SUPPORTED (`python/experiments/EXP-009/scope.md:63-66`). The analysis plan says to assert finite gate-stack MDE rows and the EXP-004 anchor (`python/experiments/EXP-009/analysis-plan.md:15-19`). The code checks EXP-003 status and file existence, but for EXP-004 it only checks that metadata exists and records whatever status is present (`python/experiments/EXP-009/code/run_experiment.py:112-136`). `load_mde_map()` loads alpha0 rows without asserting the required rows are present and finite (`python/experiments/EXP-009/code/run_experiment.py:139-156`).",
    "impact": "EXP-009 can run and produce `no_mde` or anchor-dependent outputs even when the upstream MDE map is incomplete or the EXP-004 consistency anchor did not pass. That violates the predeclared Evidence AGAINST condition and weakens reproducibility of the broadened dogfood read.",
    "fix": "Fail fast unless EXP-004 metadata records the accepted success status (`PASS` in the current metadata) and EXP-003 contains finite alpha0 MDE rows for the required referees/domains. Emit a clear dependency failure instead of allowing downstream `no_mde` classifications."
  },
  {
    "id": "F05",
    "severity": "Major",
    "title": "EXP-010 split-boundary specification conflicts with the checkpoint's timestamp discipline",
    "evidence": "The active design requires shared split boundaries as canonical `CloseTime` timestamps, never per-timeframe row fractions (`docs/experiments-docs/checkpoints/2026-06-03-002-referee-refinement-and-stringency/design.md:108`). EXP-010 scope repeats that the split unit is the shared 1-minute `CloseTime` boundary mapped into each domain, never a per-timeframe row fraction (`python/experiments/EXP-010/scope.md:42-45`). But the approved plan defines walk-forward warmup as `floor(0.5 * n)` and purged CV folds over `[0, n)` domain-row indices (`python/experiments/EXP-010/analysis-plan.md:53-64`), and the code implements `int(n * warmup_fraction)` plus `np.linspace(0, n, k + 1)` (`python/experiments/EXP-010/code/split_protocols.py:66-68`, `:85`).",
    "impact": "The experiment's artifacts disagree on whether alternative folds are timestamp-mapped or domain-row-fraction based. If row fractions are used, cross-domain protocol differences may partly reflect domain-specific aggregation calendars rather than the intended split-protocol robustness question.",
    "fix": "Resolve this before execution. Either compute fold boundaries as 1-minute analysis-set `CloseTime` timestamps and map those timestamps into each domain, or amend the scope/checkpoint to explicitly permit domain-row-index folds for EXP-010 and explain why the timestamp-boundary rule does not apply to these alternative protocols."
  },
  {
    "id": "F06",
    "severity": "Minor",
    "title": "EXP-010 does not implement the planned bounded/streamed verdict output",
    "evidence": "The scope says `protocol_draw_verdicts.csv` should be bounded/streamed to disk (`python/experiments/EXP-010/scope.md:168-175`), and the analysis plan says not to accumulate all verdict rows in memory (`python/experiments/EXP-010/analysis-plan.md:192-195`). The implementation appends every worker result into a single `verdict_rows` list (`python/experiments/EXP-010/code/run_experiment.py:243-255`) and writes the full list at the end (`python/experiments/EXP-010/code/run_experiment.py:702-703`).",
    "impact": "The current draw budget is probably still manageable, but the implementation violates its own memory-control plan and will scale poorly if draw counts or protocols increase.",
    "fix": "Write verdict rows in deterministic batches, then run summaries from the streamed CSV or from bounded aggregate accumulators. If full in-memory accumulation is intentionally acceptable for the fixed budget, amend the plan and governance text to state the expected row count and memory bound."
  }
]
```

Summary: EXP-008 is the cleanest of the three pre-execution packages; I found no
Critical or Major issue in its scoped post-processing approach. EXP-009 needs the
CI-aware MDE-location rule and dependency gate tightened before its outputs can be
trusted. EXP-010 needs revision before execution: the FPR materiality bug can invert
the headline verdict, and the multi-fold wrapper currently does not maintain a clean
train/OOS partition.
