# Handoff — EXP-101–104 analysis readiness (2026-08-20)

**Checkpoint:** `2026-08-11-019-liquidity-sweeps` (OPEN)  
**Family:** `CF-LIQSWP-001` — REGISTERED; no family decision  
**Branch:** `temp-pi-001`  
**HEAD:** `1f0315b` (`feat(EXP-101-104): AMENDMENT-16 - singleton destroy groups are disclose-only`)  
**Working tree:** dirty (fixes below are **not committed**)  
**Vehicle:** analysis-only re-read of frozen EXP-100 AMENDMENT-14 TRAIN (264 cells). No new engine, no TEST, no holdout.

Resume with `/skill:research-pipeline`. Announce this handoff. Do **not** treat unofficial `analysis_results.json` as a verdict.

---

## One-line status

Analysis code is close. **102 and 103 were approved in a fresh review this session (not written to `qa-review.md`). 104 still has two frequency bugs. 101 never finished a fresh review.** On-disk QA files still show 17 Aug **REVISE**. Operator has not approved execution.

---

## What the next session must do first

```text
1. Read this handoff + checkpoint design.md (through AMENDMENT-16).
2. Commit or inspect the dirty tree (list below) before more QA.
3. Fix EXP-104 only: timeframe key 60m, and warmup/undefined frequency exposure.
4. Fresh-context QA (subagent) on current tree — especially 101 and 104.
5. Operator execution gate. Then live analysis. Then estimand gate. Then data-analyst.
6. Do not load TEST / holdout. Do not rerun or modify EXP-100.
```

---

## Dirty / untracked (must not lose)

Uncommitted implementation (this session):

| Path | Why |
|---|---|
| `python/src/xen/liqswp_analysis/source.py` | `key_join_evidence` — null `profile_generation` matches |
| `python/src/xen/liqswp_analysis/destroy.py` | Live JSON omits the 2,000-draw list; keep mean/interval/count |
| `python/experiments/EXP-102/analysis_code/analysis.py` | Raid-id uniqueness is **per cell**, not global |
| `python/experiments/EXP-103/analysis_code/analysis.py` | Left join with `nulls_equal=True` |
| `python/experiments/EXP-104/analysis_code/analysis.py` | Gate-first frequency; prefix-sum bootstrap; per-TF blocks (still keyed `1h`) |
| `python/tests/liqswp_analysis/test_source.py` | Null-key join test |
| `python/tests/liqswp_analysis/test_exp104_adapter.py` | Bootstrap parity vs gather; speed smoke |
| `python/tests/test_exp102_analysis_live.py` | Cell-scoped duplicate raid_id |
| `python/tests/test_exp103_analysis_live.py` | Destroy summaries, not full draw list |

Untracked leftovers — **not** a pipeline result:

- `python/experiments/EXP-10{1,2,3,4}/results/analysis_results.json`
- 101: 235MB, unofficial, `blocking_pass=true` (run before later fixes)
- 102: 290MB, `VOID_DUPLICATE_RAID_ID` (false global uniqueness — now fixed in dirty code)
- 103/104: ~2KB, `VOID_PROFILE_JOIN_MISMATCH` on 868 ATR-undefined **null-null** keys (now fixed in dirty code)

Do not analyse these files. Prefer delete after the next approved live run.

---

## Binding sources (do not relitigate)

1. `docs/experiments-docs/checkpoints/2026-08-11-019-liquidity-sweeps/liquidity.md` — original idea  
2. Same folder `design.md` — frozen scope + AMENDMENT-2…16  
3. `python/experiments/EXP-10X/design.md` — experiment narrowings  
4. EXP-100 operator: ATR-undefined excursion values excluded from interpretation; no EXP-100 rerun

**AMENDMENT-15 (LOOSER, operator 2026-08-17):** destroyed non-bite uses **raw** SE.  
**AMENDMENT-16 (LOOSER, operator 2026-08-18):** singleton destroy groups stay fixed and are disclosed; void only `VOID_NO_MOVABLE_ROWS` / `VOID_NO_CHANGED_VALUE`.  
Ledger: **4 looser / 3 tighter / 8 neutral**.

cTrader TRAIN only: EURUSD, XAUUSD, USTEC. Fence INFR-021. Zero-cost. Observation-bar raids. Tight gap: `gap_span < 0.50 * VA_width`.

Live one-hour **observation** cells are named and labelled **`60m`**, not `1h` (confirmation reference is still `1H`/`4H`).

---

## Per experiment

### EXP-101 — level config vs later swing (HYP-001)

- Independent arm resampling; comparators `PREVIOUS_1H` / `PREVIOUS_ASIA` / `ROLLING_7`.
- Stratum columns omit `config` so both arms share a partition.
- Shared destroy/source apply. No EXP-101-specific dirty file.
- **QA:** last on-disk run 11 = REVISE (17 Aug). This session’s reviewer **never finished**. Treat as **not approved**.
- Unofficial live JSON exists; ignore.

### EXP-102 — prior-raid count (HYP-002)

- Joint resampling; bands `0` / `1` / `2+` vs `0`.
- Dirty: uniqueness on `(source_cell, raid_id)` or cell-identity columns — BREAKOUT_BAR and LEVEL_CLOSE **share** `raid_id` by EXP-100.
- Empty donor / empty arm: guarded in `destroy.py` (`n_rows==0`, empty stack). Confirm in QA.
- **QA this session (chat only): APPROVE.** File not appended.

### EXP-103 — tight vs non-tight TPO gap (HYP-003)

- Left join `(source_cell, raid_id, profile_generation)`.
- Dirty: `nulls_equal=True` so 868 ATR-undefined null generations **match**, not void.
- Outcome = completed primary **DEFINED**; tight minus non-tight; all-defined is disclosure only.
- **QA this session (chat only): APPROVE.** File not appended. Prior run-9 REVISE items (seal / VA 70% / 30% gap mass) were treated as closed by this reviewer; next QA should confirm `source.py` seal fields.

### EXP-104 — volatility regime (HYP-004)

- Outcomes: LOW/HIGH vs MID, joint resampling.
- Frequency: preceding mark; descriptive; **not** under future-destroy.
- Dirty: integrity/join **before** frequency bootstrap; prefix-sum bootstrap (parity-tested vs circular gather). **Do not port to Rust.**
- **Still wrong:**
  1. `FREQUENCY_BLOCKS_BY_TIMEFRAME` keys `"1h"`; live `timeframe` is `"60m"` → fallback `(24,48,96)` and **12 never runs**. Fix: map `"60m"` (and `"1h"`) to `(12, 24, 48)`. File: `analysis.py` ~56–60 and `_partition_payloads`.
  2. `marked_exposure` filters to LOW/MID/HIGH **before** census (`live_frame` ~535). Warmup/undefined exposure and `EMPTY_EXPOSURE` rows are dropped. Design §3: report warmup/undefined separately; do not convert them to an arm.
- **QA this session (chat only): REVISE** for those two. Gate-first and prefix-sum were accepted as the same estimator.

---

## Shared analysis library (`python/src/xen/liqswp_analysis/`)

Used by all four. A destroy bug hits every EXP.

| Piece | Rule |
|---|---|
| Nested destroy | Closed-form mean inside 10k cluster populations; live bite is 2,000 draws vs **raw** SE (A-15) |
| Singletons | Fixed + disclosed (A-16) |
| JSON | Do not dump 2,000 shuffled values into `analysis_results.json` |
| Join helper | `key_join_evidence` / `nulls_equal=True` |

Tests run this session (subset): `test_source`, `test_exp102_analysis_live`, `test_exp103_analysis_live`, `test_exp104_adapter`, `test_destroy`, `test_exp10x_analysis_contract` — **52 passed** after the four fixes; EXP-104 adapter tests **6 passed** after prefix-sum.

---

## Pipeline position

| Stage | 101 | 102 | 103 | 104 |
|---|---|---|---|---|
| Design | freeze + A-15/16 | same | same | same |
| QA APPROVE on disk | no | no | no | no |
| QA this session | incomplete | approve (chat) | approve (chat) | revise (chat) |
| Operator execution | pending | pending | pending | blocked on 104 fixes |
| Live analysis | unofficial junk JSON | junk JSON | junk JSON | junk JSON |
| `analysis.md` / `report.md` | none | none | none | none |

Indexes still say “QA REVISE FINDINGS REPAIRED — operator execution pending” — **ahead of on-disk QA**.

---

## Explicit non-goals for the next session

- No EXP-100 rerun or emission edit  
- No TEST / holdout  
- No family status change  
- No tradable / cost-complete claims  
- No Rust port of the frequency bootstrap  

---

## Suggested first prompts

```text
Resume EXP-101–104 from docs/superpowers/plans/2026-08-20-exp-101-104-handoff.md.
1) Commit or stash? 2) Fix EXP-104 60m + warmup exposure. 3) Fresh QA.
```
